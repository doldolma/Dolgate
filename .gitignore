node_modules
dist
dist-*
out
.DS_Store
*.log
coverage
apps/desktop/.vite
apps/desktop/.electron-vite
apps/desktop/.cache
apps/desktop/out
apps/desktop/test-results
apps/desktop/playwright-report
# 브라우저 도구가 남기는 스크린샷·페이지 덤프. 저장소 루트에 쌓인다.
.playwright-mcp
apps/desktop/release
apps/desktop/src/renderer/vite-env.d.ts
services/ssh-core/bin
services/ssh-core/build
services/rdp-core/target
services/vnc-core/target
# core-framing 은 두 사이드카의 path 의존이라 의존하는 쪽에서 빌드하면 target 이 생기지 않는데,
# 이 크레이트의 테스트(#[test])는 여기서 직접 돌려야만 실행된다 — 그때 자기 target 이 생긴다.
services/core-framing/target
# 벤더링한 크레이트를 그 디렉터리에서 직접 빌드·테스트하면 각자 target 이 생긴다(합쳐서 GB 단위).
services/rdp-core/vendor/*/target
apps/mobile/ios/GoSshEngine/SshCoreEngine.xcframework
services/sync-api/bin
apps/desktop/config/development.json
apps/desktop/config/desktop.json
services/sync-api/config/default.json
services/sync-api/config/production.json
services/sync-api/data
services/sync-api/=busy_timeout*
*.db
*.sqlite
*.sqlite3
*.test
.idea
.tmp
/tmp/
/data.json

# 랜딩페이지 영어판 — web/build-en.mjs 가 배포 때 생성한다.
web/en/

# 개발용 자격증명. scripts/probe.mjs 같은 도구가 읽는다.
.env.local
.env.local.*
**/.env.local
# 예시 파일은 커밋한다 — 위 .env.local.* 가 이것까지 잡으므로 명시적으로 되돌린다.
!**/.env.local.example
# generate:icons 가 .icns 를 만들 때 쓰는 중간 산출물. 결과물(.icns/.ico/.png)만 커밋한다.
apps/desktop/build/icons/dolssh.iconset

.kiro

# react-native-remote-desktop Android native/Gradle build outputs.
packages/react-native-remote-desktop/android/.cxx
packages/react-native-remote-desktop/android/build

# 기기별 권한 설정 — 개발자마다 다르다
.claude/settings.local.json
