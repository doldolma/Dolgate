{
  "name": "dolssh",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:desktop dev:api",
    "dev:desktop": "npm run dev --workspace @dolssh/desktop",
    "dev:api": "cd services/sync-api && go run ./cmd/api",
    "build": "npm run build --workspace @dolssh/desktop",
    "test": "npm run test --workspace @dolssh/desktop && cd services/ssh-core && go test ./... && cd ../sync-api && go test ./...",
    "typecheck": "npm run typecheck --workspace @dolssh/desktop"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
