# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.89"
name = "ironrdp-rdpdr-native"
version = "0.7.0"
authors = [
    "Devolutions Inc. <infos@devolutions.net>",
    "Teleport <goteleport.com>",
]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Native RDPDR static channel backend implementations for IronRDP"
homepage = "https://github.com/Devolutions/IronRDP"
readme = "README.md"
keywords = [
    "rdp",
    "remote-desktop",
    "network",
    "client",
    "protocol",
]
categories = ["network-programming"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/Devolutions/IronRDP"
resolver = "2"

[lib]
name = "ironrdp_rdpdr_native"
path = "src/lib.rs"
# PATCH (Dolgate): 상류는 test = false 로 자기 테스트 하네스를 껐다. 그래서 이 크레이트에 있던
# 경로 탈출 방어 테스트(containment_tests)가 한 번도 실행되지 않았다. 우리 패치가 보안에 걸리는
# 부분(장치별 루트 해석)을 다루므로 켠다 — `cargo test -p ironrdp-rdpdr-native` 로 돈다.
test = true
doctest = false

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.ironrdp-core]
version = "0.2"

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.ironrdp-pdu]
version = "0.9"

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.ironrdp-rdpdr]
version = "0.7"

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.ironrdp-svc]
version = "0.8"

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.nix]
version = "0.31"
features = [
    "fs",
    "dir",
]

[target.'cfg(any(target_os = "macos", target_os = "linux"))'.dependencies.tracing]
version = "0.1"
features = ["log"]
